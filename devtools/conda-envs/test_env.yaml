name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # package
  - numpy
  - rdkit

    # Pip-only installs
  #- pip:
  #  - codecov

